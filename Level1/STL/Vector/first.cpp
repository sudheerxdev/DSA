#include <iostream>
#include <vector>
using namespace std;
int main() {
    vector<int> arr;
    // adding the element in vector
    arr.push_back(12);
    arr.push_back(12);
    cout << arr[0];
    cout << arr[1];
}
